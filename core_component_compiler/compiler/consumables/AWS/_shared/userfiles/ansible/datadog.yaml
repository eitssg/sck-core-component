# -------------------------------------------
# THIS FILE WAS GENERATED BY AWS PIPELINE
# -------------------------------------------

- hosts: localhost
  become: yes
  become_method: sudo
  vars:
    datadog_api_key: '<datadog_api_key>'
    datadog_config:
      proxy:
        https: '<datadog_proxy>'

  roles:
    - datadog
